# Pinned runtime dependencies, as tested. For a reproducible install:
#   pip install -r requirements.txt && pip install --no-deps -e .
# pyproject.toml keeps compatible ranges (>=); this file freezes the tested set.
pydantic==2.13.4
pydantic-settings==2.14.2
httpx==0.28.1
fastapi==0.139.2
starlette==1.3.1
uvicorn[standard]==0.51.0
SQLAlchemy==2.0.51
aiosqlite==0.22.1
python-jose[cryptography]==3.5.0
cryptography==49.0.0
fpdf2==2.8.7
pypdf==6.16.2
openpyxl==3.1.5
