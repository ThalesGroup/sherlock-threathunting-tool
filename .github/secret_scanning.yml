# GitHub secret scanning: the test suite contains fake credentials on purpose
# (they verify the masking layer). Real configuration lives outside the repository.
paths-ignore:
  - "tests/**"
  - "web/src/**/*.test.ts"
